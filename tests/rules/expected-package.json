{
  "description": "Expected identity of the released solution package. The identity values are now asserted rather than recorded: the 1.1.0.5 baseline package was supplied to the 1.1.0.6 cycle, so the authoritative solution and publisher names are known and a change to either must fail the build.",

  "expectedVersion": "1.1.0.6",
  "expectedManaged": false,
  "expectedCanvasAppName": "ESG Radiological Surveys",
  "expectedCanvasWidth": 1440,
  "expectedCanvasHeight": 960,
  "expectedCanvasOrientation": "Landscape",

  "expectedSolutionUniqueName": "ESGDigitalRadiologicalSurveys",
  "expectedPublisherUniqueName": "Cr837da",

  "requiredSolutionFiles": [
    "solution.xml",
    "customizations.xml",
    "[Content_Types].xml"
  ]
}
